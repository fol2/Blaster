# Blaster

Blast all the baddies.

## Play

Open `index.html` in a browser.

## Test

Open `test-game-data.html` in a browser. The page shows pass or fail results for the key progression and catalogue rules.
